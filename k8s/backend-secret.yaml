apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: back
  labels:
    app: backend
type: Opaque
stringData:
  # Full connection string (contains credentials - keep in Secret, not ConfigMap)
  # PostgreSQL (local / cloud)
  DATABASE_URL: "postgresql://tushar:Tt%408791600229@db-poc12.postgres.database.azure.com:5432/postgres?sslmode=require"

  # Azure SQL Server
  # DATABASE_URL: "mssql://admin:YourPassword@server.database.windows.net:1433/mydb"

  # Azure Cosmos DB / MongoDB Atlas
  # DATABASE_URL: "mongodb+srv://user:pass@account.mongo.cosmos.azure.com/?retryWrites=true"

  # MySQL
  # DATABASE_URL: "mysql://root:password@mysql:3306/myapp_db"
